# Reviewer 报告｜Word Image（ON）认知减负调整

**审查日期：** 2026-09-01
**任务等级：** T3
**审查对象：** `website/app.js`、`website/app.test.js`、`website/styles.css` 的未提交 Word Image 改动
**审查身份：** 独立 Reviewer；未参与本次实现。

## 目标与范围

本次只调整 Word Image 的第 3 步信息顺序，不改变课程数据、三步路径、进度 schema 或其它模块：

1. 将整词美式音标 `/ɑn/` 与“听 ON”放在同一处；
2. 移除与逐词音标重复的整句“清晰美式音标”层；
3. 保留英文原句、逐词音标、自然语流，以及连读、弱读、重音、意群和节奏说明；
4. 让核心图完整显示，并保持窄屏可读。

## 结论：PASS

Owner 已在当前页面完成体验验收并于 2026-09-01 回复“通过”。独立 Reviewer 未发现 Critical、Important 或 Minor 问题；本次改动可以提交。

## 检查结果

| 检查项 | 结论 | 证据 |
| --- | --- | --- |
| 整词发音入口更直接 | PASS | 整词 IPA `/ɑn/` 与“听 ON”同处；保留原有语音 action 和不可用时的禁用降级。 |
| 信息重复被移除 | PASS | 不再渲染独立的整句“清晰美式音标”层；逐词对应仍保留。 |
| 真实语流学习链未丢失 | PASS | 英文原句、逐词 IPA、自然语流、展开的连读/弱读/重音/意群/节奏说明仍在同一学习链中。 |
| 核心画面与窄屏结构 | PASS | 核心图使用完整显示；窄屏保持单列与紧凑间距。 |
| 三步路径与进度隔离 | PASS | 未改动进度写入、步骤推进或跨模块状态逻辑；相关回归测试通过。 |
| 自动验证 | PASS | `node --test website/app.test.js`：157 通过、0 失败；`git diff --check` 通过。 |
| 人工体验验收 | PASS | Owner 已在本地页面实际操作并确认通过。 |

## 必须修复（FAIL）

无。

## 证据边界

本环境无法自动接管本地 `file://` 页面进行截图与点击回放；人工体验结论以 Owner 的实际验收为准。对外发布时仍应按 PRD 补充 1920px、1180px、1024px、375px 的发布记录。

## LEARN

- **Test：** 信息层级调整必须同时断言“被移除的重复层不存在”与“保留的关键学习层仍按顺序存在”。
- **Golden：** `ON` 样板继续作为 Word Image 的验收参考；“整词声音入口紧邻整词音标、句子层只保留逐词对应与自然语流”成为该样板的阅读顺序。
- **Skill：** 无新增专用 Skill；现有 Builder / Reviewer 分离与 Owner 验收闭环有效。
